using UnityEngine;
using System.Collections;

public class FlagReset : MonoBehaviour {

    // Removes flags to instantiate new onces
	void Start () {
        //Destroy(gameObject, WHEN CORRECT AND INCORRECT FLAG HAS BEEN PICKED);
	}
}